#!/bin/sh
if [ -z $COMPILER_PATH ]; then
    echo WARNING: `pwd`: uclibc-addr2line $@ diverted to cross-toolchain >> ${SRM_ROOT}/cross.warnings
fi
exec ${SRM_BUILD_DIR}/bin/cross-uclibc-addr2line "$@"
